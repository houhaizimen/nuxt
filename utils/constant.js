let baseUrl
// if (process.env.NODE_ENV === 'development') {
  baseUrl = 'http://127.0.0.1:3002/'
  // baseUrl = 'http://47.101.134.233:3002/'
// } else {
  // 正式环境
  // baseUrl = 'http://' + location.hostname +'/'
// }

export default {
  baseUrl
}